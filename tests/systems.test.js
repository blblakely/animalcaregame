import {describe,it,expect} from 'vitest';import {AnimalSystem} from '../src/systems/AnimalSystem.js';import {EconomySystem} from '../src/systems/EconomySystem.js';import {AdoptionSystem} from '../src/systems/AdoptionSystem.js';import {IntakeSystem} from '../src/systems/IntakeSystem.js';import {EnrichmentSystem} from '../src/systems/EnrichmentSystem.js';
describe('core game systems',()=>{it('creates all starter species and a medication tutorial',()=>{const s=new AnimalSystem(()=>.2);const animals=s.seed();expect(animals.map(a=>a.species)).toEqual(['Dog','Cat','Rabbit']);expect(animals[2].medicationNeeded).toBe(true)});it('care improves needs without exceeding 100',()=>{const s=new AnimalSystem(()=>.2);const a=s.create('Dog','dog');a.hunger=90;s.care(a,'feed');expect(a.hunger).toBe(100);a.medicationNeeded=true;s.care(a,'medicate');expect(a.medicationNeeded).toBe(false)});it('economy charges costs and rewards adoption',()=>{const e=new EconomySystem();expect(e.spend(3)).toBe(true);expect(e.money).toBe(117);const reward=e.adoption(100);expect(reward).toBe(70);expect(e.adoptions).toBe(1);expect(e.reputation).toBeGreaterThan(1)});it('matching rewards shared preferences',()=>{const a={species:'Cat',traits:['Calm','Friendly']};const sys=new AdoptionSystem();expect(sys.score(a,{species:'Cat',activity:'Calm',wish:'Friendly'})).toBe(100);expect(sys.score(a,{species:'Dog',activity:'Playful',wish:'Independent'})).toBe(25)})});
describe('new management systems',()=>{it('keeps a stable appearance and enforces intake capacity',()=>{const animals=new AnimalSystem(()=>.1);animals.seed();const intake=new IntakeSystem(animals,()=>.1);intake.capacity=3;const candidate=intake.generate();expect(intake.accept(candidate.id)).toBeNull();expect(animals.animals[0].appearance.id).toBe('golden')});it('purchases and places useful enrichment',()=>{const animals=new AnimalSystem(()=>.1);const animal=animals.create('Dog','dog');const economy=new EconomySystem();const shop=new EnrichmentSystem(economy);expect(shop.purchase('puzzle-feeder').name).toBe('Puzzle Feeder');expect(shop.place('puzzle-feeder',animal)).toBe(true);expect(animal.enrichment).toContain('puzzle-feeder');expect(economy.money).toBe(98)})});

